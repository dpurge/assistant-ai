# Typst format for research reports (stub)

This file will define:

- The Typst **template** for a research-report PDF: page geometry, fonts, headings.
- Mapping from report sections (summary, body, open questions, references) to Typst blocks.
- Citation rendering — likely footnotes, or end-of-document references list with anchor links.
- Optional cover page and table of contents for long reports.

To be filled in when `research-typst` is implemented.
